"""
Analyst review node (human-in-the-loop).

Implement analyst_review_node(state): pause the graph with LangGraph's interrupt() so an analyst can
sign off on the recommendation or override it, and store the resumed action. See the problem 
description for the interrupt payload keys and the return contract.
"""
