"""
Market analysis node (Stage 1, parallel).

Implement market_node(state): run Market TrendsAgent on the property record and write the result to 
this node's OWN result key. Skip when the record was already filed as a no-action call at 
validation, and record a failure in errors instead of raising. See the problem description for the 
return contract.
"""
