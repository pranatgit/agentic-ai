"""
Report node.

Implement report_node(state): assemble the final investment report, folding the analyst's decision 
into the outcome - a sign-off confirms the pipeline's call, an override replaces it, and no analyst 
input keeps the pipeline's own call. See the problem description for the report keys, the decision 
tokens, and the return contract.
"""
