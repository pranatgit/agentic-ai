"""
Application entry point and the application logic to implement.

Run (from the Project folder): 
    python3 -m streamlit run main.py

Implement run_analysis (property_record), approve_decision(thread_id) and override_decision(thread_id), 
then render the pre-loaded browser interface with them. The interface is preloaded in streamlit_UI.py 
- it receives these functions and calls them when the analyst clicks buttons. See the problem 
description for the initial state, the thread_id contract, when an analysis is recorded to the store, 
and the interface call.
"""
